//- Your description here. All lines starting with "//- " or "'- "//- will be automatically used as the default description when/if//- you upload your script, saving a lot of typing. This is optional, of course.// The following items, starting with "//#" or "'#", will automatically// replace the corresponding items in the script upload dialog, saving a// lot of typing. These lines are, of course, optional.//# Author: Your name here//# E-Mail: your@address.here//# Name: If different from script name//# Category: Category|Hierarchy|Here//# MinVersion: 2.0//# Keywords: List, of, comma-separated, Keywords//# URL: http://your.site.url.hereclass ScopeControl	implements TelescopeControl		function CanGet(item as integer) as boolean		// Return true if the telescope can get the given parameter		return false	end function		function CanSet(item as integer) as boolean		// Return true if the telescope can set the given parameter		return false	end function		function Get(item as integer, params() as string) as string		// Get a telescope parameter		return ""	end function		function Set(item as integer, params() as string) as string		// Set a telescope parameter		return ""	end function		sub Restore()		// Restore volatile parameters (if necessary)	end sub		sub Save()		// Save volatile parameters (if necessary)	end subend classsub main()	// main() will be called when the script starts running	// All functionality is handled by the TelescopeController.Run() method. 	// The user is expected to define a class that implements the TelescopeControl interface, and pass	// an instance of that class into the TelescopeController.Run() method.		TelescopeController.Run(new ScopeControl)end sub